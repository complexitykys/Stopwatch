<resources>
    <!-- Base application theme. -->
    <style name="Theme.Stopwatch.Day" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Customize your theme here. -->
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:windowTranslucentStatus">false</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
    </style>

    <style name="ButtonStyle" parent="Widget.AppCompat.Button">
        <item name="android:layout_width">@dimen/d150dp</item>
        <item name="android:layout_height">@dimen/d50dp</item>
        <item name="android:background">@drawable/button_dark_style</item>
        <item name="android:textAllCaps">false</item>
        <item name="android:textColor">@color/gray</item>
        <item name="android:textSize">@dimen/s16sp</item>
    </style>

    <style name="BackGround" parent="Theme.Stopwatch.Day">
        <item name="android:background">@color/white</item>
    </style>

    <style name="TextColor" parent="Theme.Stopwatch.Day">
        <item name="android:textColor">@color/gray</item>
    </style>

    <style name="ImageButtonIco" parent="Theme.Stopwatch.Day">
        <item name="android:src">@drawable/ic_dark_mode</item>
    </style>

</resources>