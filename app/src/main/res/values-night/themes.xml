<resources>
    <!-- Base application theme. -->
    <style name="Theme.Stopwatch.Night" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_200</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/black</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_200</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Customize your theme here. -->
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:windowTranslucentStatus">false</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
    </style>

    <style name="ButtonStyle" parent="Widget.AppCompat.Button">
        <item name="android:layout_width">@dimen/d150dp</item>
        <item name="android:layout_height">@dimen/d50dp</item>
        <item name="android:background">@drawable/button_light_style</item>
        <item name="android:textAllCaps">false</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:textSize">@dimen/s16sp</item>
    </style>

    <style name="BackGround" parent="Theme.Stopwatch.Night">
        <item name="android:background">@color/gray</item>
    </style>

    <style name="TextColor" parent="Theme.Stopwatch.Night">
        <item name="android:textColor">@color/white</item>
    </style>

    <style name="ImageButtonIco" parent="Theme.Stopwatch.Night">
        <item name="android:src">@drawable/ic_light_mode</item>
    </style>

</resources>